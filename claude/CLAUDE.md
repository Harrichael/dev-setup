# Global instructions

Imported into `~/.claude/CLAUDE.md` by dev-setup's `install.sh`, so these apply
to every Claude Code session on this machine regardless of project.

Keep this file for things that are true everywhere. Anything specific to one
repo belongs in that repo's own `CLAUDE.md`.

<!-- Replace everything below. -->

CANARY-DEV-SETUP-GLOBAL-OK
