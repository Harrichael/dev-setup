# Global instructions

Imported into `~/.claude/CLAUDE.md` by dev-setup's `install.sh`, so this applies
to every Claude Code session on this machine, in every project.

Keep this file for what's true everywhere. Anything specific to one repo belongs
in that repo's own `CLAUDE.md` instead.
