# NeoVim nvim_create_autocmd is nil (cannot be called)

Install latest version of neovim, some distros may not have latest stable build

```
sudo add-apt-repository ppa:neovim-ppa/stable
sudo apt update
```
